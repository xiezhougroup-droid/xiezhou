# 云端稳定部署说明

当前 `lhr.life` 临时隧道不稳定，适合几分钟演示，不适合给同事反复测试。更稳定的方式是把网页原型部署到云端，生成一个固定网址。

## 推荐方案

前期推荐用 Render 的 Docker Web Service：

- 同事不需要和你在同一网络；
- 你不用一直开着本机电脑；
- 会得到一个固定网址；
- 后续也方便交给程序员继续整理。

## 部署前准备

这个文件夹已经包含云端部署需要的文件：

- `Dockerfile`
- `render.yaml`
- `requirements.txt`
- `web_app.py`
- `src/`
- `config/`

## 操作步骤

1. 把 `采购询价本地工作流` 文件夹上传到一个 GitHub 仓库。
2. 打开 Render，新建 `Web Service`。
3. 选择这个 GitHub 仓库。
4. Environment 选择 `Docker`。
5. 部署完成后，Render 会生成一个 `https://...onrender.com` 的固定网址。
6. 把这个网址发给同事使用。

## 使用注意

- 当前原型只支持 `.xlsx`，旧版 `.xls` 请先用 Excel/WPS 另存为 `.xlsx`。
- 免费云服务可能会休眠，第一次打开会慢一点，但不会像临时隧道那样频繁换链接。
- 云端免费版的文件存储通常不是永久数据库，适合演示和流程验证。正式上线时建议接对象存储或数据库。
- 不建议在临时演示环境上传特别敏感的真实报价文件。

## 正式上线建议

等流程稳定后，建议改成：

- 固定云服务器或公司内网服务器；
- 登录账号和权限；
- 文件统一存储；
- 操作记录和版本留痕；
- 供应商报价文件隔离；
- 每个项目一个独立工作区。
