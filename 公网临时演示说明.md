# 公网临时演示说明

这个入口用于最快给异地同事演示网页原型。它会在你的电脑上启动采购询价智能助手，并通过临时公网隧道生成一个外网链接。

## 使用方法

Mac：

1. 双击 `启动公网临时演示.command`。
2. 等待终端里出现 `https://` 开头的网址。
3. 把这个网址发给同事。
4. 演示结束后关闭终端窗口，链接会自动失效。

Windows：

1. 双击 `启动公网临时演示-Windows.bat`。
2. 等待窗口里出现 `https://` 开头的网址。
3. 把这个网址发给同事。
4. 演示结束后关闭窗口，链接会自动失效。

## 注意事项

- 这个方式需要你的电脑能访问外网，并且公司网络允许 `ssh` 连接。
- 这是临时演示方案，不建议上传特别敏感的真实文件。
- 链接有效期间，拿到链接的人都可以访问这个网页。
- 如果窗口里没有出现公网链接，通常是网络或防火墙拦截了 `ssh`。这种情况需要改用云服务器或正式部署方案。
