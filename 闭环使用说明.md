# 采购询价智能体本地闭环使用说明

## 当前闭环

当前本地原型已跑通以下流程：

1. 按样板文件生成采购包拆分总表；
2. 按采购包批量生成报价邀请包和报价清单；
3. 采购员回收供应商报价后，自动合并报价并生成谈判策略；
4. 采购员谈判后填写反馈表；
5. 系统根据采购员确认结果输出推荐合作结果。

## 一键运行

```bash
./采购询价本地工作流/run_full_loop.command
```

## 分步运行

### 1. 按样板标准拆分采购包

```bash
./采购询价本地工作流/run_split_from_standard.command
```

输出：

```text
采购询价本地工作流/output/时间戳_按昨日样板标准拆分/
```

### 2. 批量生成全部采购包邀请清单

```bash
./采购询价本地工作流/run_generate_invitations.command --packages all
```

只生成指定采购包：

```bash
./采购询价本地工作流/run_generate_invitations.command --packages 电线电缆包,配电箱柜包
```

### 3. 回收供应商报价并分析

把供应商返回的报价 Excel 放到：

```text
采购询价本地工作流/input/supplier_quotes/
```

建议结构：

```text
supplier_quotes/
  电线电缆包/
    供应商A-报价清单.xlsx
    供应商B-报价清单.xlsx
```

然后运行：

```bash
./采购询价本地工作流/run_analyze_quotes.command
```

输出：

```text
供应商报价分析及谈判策略.xlsx
```

### 4. 谈判后填写反馈并出推荐

谈判反馈表：

```text
采购询价本地工作流/input/negotiation_feedback.xlsx
```

填写字段包括：

- 采购包
- 供应商
- 最终报价总价
- 谈判后付款条件
- 谈判后供货周期
- 技术/品牌确认情况
- 采购员确认状态
- 推荐意见
- 风险备注

然后运行：

```bash
./采购询价本地工作流/run_finalize_recommendation.command
```

输出：

```text
采购推荐合作结果.xlsx
```

## 重要口径

- 系统不会覆盖原始文件；
- 追溯号是报价回填和比价分析的关键字段，不能删除；
- 缺少关键参数的清单项进入待补充确认项，不应直接发供应商正式报价；
- 最终推荐必须经过采购员确认，系统只输出分析和建议，不自动替采购员定标。
