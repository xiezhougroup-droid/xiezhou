# 给同事使用说明

## Windows 电脑

如果负责人已经启动“局域网网页助手”，同事不需要安装任何东西，只要打开负责人发来的局域网网址即可，例如：

```text
http://192.168.1.23:8876
```

下面是同事自己电脑独立运行时才需要的步骤。

第一次使用：

1. 安装 Python 3.11 或以上版本；
2. 安装时勾选 `Add Python to PATH`；
3. 双击 `安装依赖-Windows.bat`；
4. 双击 `启动网页助手-Windows.bat`。

日常使用：

- 双击 `启动网页助手-Windows.bat`；
- 浏览器会自动打开 `http://127.0.0.1:8876`；
- 第一步上传需要采购询价的原始清单 Excel；
- 然后在网页上点按钮、下载结果；
- 结果会自动生成到 `output` 文件夹。

## Mac 电脑

第一次使用：

1. 确认电脑已安装 Python 3；
2. 如缺少依赖，在终端运行：

```bash
python3 -m pip install -r requirements.txt
```

日常使用：

- 双击 `启动网页助手.command`；
- 浏览器会自动打开 `http://127.0.0.1:8876`；
- 第一步上传需要采购询价的原始清单 Excel；
- 然后在网页上点按钮、下载结果。

## 网页打不开怎么办

如果浏览器没有自动打开，手动在浏览器地址栏输入：

```text
http://127.0.0.1:8876
```

注意：启动窗口不能关闭，关闭窗口就会停止网页服务。

## 第一步上传什么

第一步上传的是“需要采购询价、尚未经过分类的清单文件”，不是样板文件。

网页下方的“高级设置：上传分类标准样板”一般不用管，只有负责人要更新后台分类口径时才使用。

## 供应商报价怎么放

把供应商返回的报价 Excel 放到：

```text
input/supplier_quotes/
```

建议按采购包建文件夹：

```text
input/supplier_quotes/电线电缆包/供应商A-报价清单.xlsx
```

然后在网页中点击：

```text
分析报价
```

## 谈判反馈怎么填

网页中可以下载或上传谈判反馈表。

```text
谈判反馈表
```

填写后再点击：

```text
生成推荐结果
```

## 注意

- 不要删除报价清单里的 `追溯号`；
- 不要直接改 `src` 文件夹里的脚本；
- 如果运行失败，把报错截图发回给负责人。
