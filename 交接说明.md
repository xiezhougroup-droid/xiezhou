# 采购询价本地脚本工作流交接说明

## 交付目标

把当前人工打样形成的采购询价流程整理成可重复运行的本地脚本：

1. 输入正式询价清单底表、项目公共信息表、图纸文件夹；
2. 按指定供应商包生成报价邀请包；
3. 同时输出图纸资料索引、附件匹配表和人工确认项；
4. 规则尽量放在配置文件中维护；
5. 不覆盖原始输入文件。

## 当前默认样例

- 供应商包 key：`baiye_fengkou`
- 输出口径：`百叶风口及防雨百叶采购报价包`
- 底表来源字段：`铝合金门窗供应商包`
- 默认底表：`正式询价清单底表-第二版.xlsx`
- 默认公共信息表：`项目公共信息确认表.rtf`
- 默认图纸目录：`图纸/`

## 目录结构

- `src/run_workflow.py`：主脚本；
- `config/workflow.json`：默认输入、输出和 sheet 名配置；
- `config/packages.json`：供应商包规则、正文口径、附件关键词配置；
- `input/`：预留给员工后续集中放输入文件；
- `output/`：每次运行生成一个新的时间戳目录；
- `README.md`：给使用者的运行说明。

## 运行方式

在项目根目录执行：

```bash
./采购询价本地工作流/run_workflow.command
```

或临时指定输入：

```bash
./采购询价本地工作流/run_workflow.command \
  --boq 正式询价清单底表-第二版.xlsx \
  --public-info 项目公共信息确认表.rtf \
  --drawings 图纸 \
  --package baiye_fengkou
```

## 本次验证结果

当前已使用默认配置跑通过一次，生成结果位于：

`采购询价本地工作流/output/20260529_154316_baiye_fengkou/`

本次统计：

- 正式报价项：22；
- 清单需人工确认项：3；
- 图纸 PDF 索引：267；
- 附件匹配总数：14；
- 建议直接挂接附件：5；
- 候选需确认附件：9；
- 图纸人工确认项：21。

## 仍建议程序员后续整理的部分

1. 把命令行脚本包成一个员工可用的小界面；
2. 把公共信息表改成标准 Excel 或网页表单，减少 RTF 解析不稳定性；
3. 把供应商包规则维护做成配置页；
4. 引入更强的 PDF 标题栏识别，不只依赖文件名；
5. 下一阶段增加供应商报价识别和基于追溯号的回填流程；
6. 增加异常提示，例如底表字段缺失、图纸目录为空、供应商包 key 不存在等。
